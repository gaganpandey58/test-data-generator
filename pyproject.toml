[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "test-data-generator"
version = "0.1.0"
description = "Configuration-driven test data generation for entities."
requires-python = ">=3.12"
dependencies = [
    "Faker>=26",
    "jsonschema>=4.23",
    "orjson>=3.10",
]

[project.scripts]
generate-data = "test_data_generator.cli:run_default"

[project.optional-dependencies]
dev = [
    "ruff>=0.6",
    "mypy>=1.11",
]

[tool.hatch.build.targets.wheel]
packages = ["src/test_data_generator"]
include = ["src/test_data_generator/**/*.json"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "D", "B"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/test_data_generator"]
